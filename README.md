# Task Manager

Full-stack task manager with role-based access.

## Setup

- Backend:
  ```bash
  cd backend
  uvicorn app.main:app --reload
  ```
- Frontend:
  ```bash
  cd frontend
  npm run dev
  ```
